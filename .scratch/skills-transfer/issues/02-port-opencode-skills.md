# 02 — Port opencode skills (4 shared)

**What to build:** Install the 4 shared opencode skills (`caveman`, `grill-me`, `toggle-model-provider`, `actual-budget-cli`) from `dotfiles:.config/opencode/skills/` into `~/.config/opencode/skills/`; verify `opencode skill list` shows all four.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] All 4 skill directories copied to `~/.config/opencode/skills/`
- [ ] `opencode skill list` includes caveman, grill-me, toggle-model-provider, actual-budget-cli
- [ ] No file conflicts with existing local skills