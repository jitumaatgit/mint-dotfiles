# 06 — Sync opencode.nix commands (optional update)

**What to build:** Compare current `home/opencode.nix` embedded opencode commands against dotfiles source (`dotfiles:.config/opencode/commands/*.md` + `docs.md` mode); update if drifted.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] Diff generated between `home/opencode.nix` and dotfiles command sources
- [ ] If drifted: `home/opencode.nix` updated with latest command definitions
- [ ] If in sync: no changes needed; recorded as verified