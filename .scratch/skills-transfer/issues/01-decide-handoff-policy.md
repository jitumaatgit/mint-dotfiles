# 01 — Decide handoff policy

**What to build:** Choose whether to port the customized `handoff` skill from the dotfiles repo (with Linux path `/home/mint/notes/handoff/`, `disable-model-invocation: false`, RESOLVED-prefix/verify checklist) or keep the upstream fresh install (saves to OS temp dir, adds redaction/"suggested skills" guidance).

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] Decision recorded: "port customized" or "keep upstream"
- [ ] If porting: Linux path chosen and verified writable
- [ ] If keeping upstream: no file operations needed for handoff